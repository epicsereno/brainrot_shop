<span id="cart_badge">
  <% if cart.count.positive? %>
    <span class="absolute -right-1 -top-1 inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-fuchsia-600 px-1.5 text-xs font-bold text-white">
      <%= cart.count %>
    </span>
  <% end %>
</span>
